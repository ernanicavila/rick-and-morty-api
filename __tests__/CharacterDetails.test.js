// import characterService from '../src/services/Characters';
// import { fetchMock, fetchRick, fetchAll } from '../utils/mock';

describe('Verifica as funções de API', () => {
	beforeEach(() => jest.clearAllMocks());
	it('', () => {});
	// it('Confere list', async () => {
	// 	const service = await characterService.list({
	// 		search: 'Rick Sanchez',
	// 		page: 0,
	// 	});

	// 	expect(service.data.results).toEqual(fetchRick);
	// });

	// it('Confere list sem parametros', async () => {
	// 	const service = await characterService.list({});
	// 	expect(service.data.results).toEqual(fetchAll);
	// });
	// it('Confere getById', async () => {
	// 	const service = await characterService.getById(1);

	// 	expect(service.data).toEqual(fetchMock[0]);
	// });
});
